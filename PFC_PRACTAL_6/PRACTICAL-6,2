#include <stdio.h>
void sumEven() {
    int n, sum = 0;
    printf("Enter n: ");
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        if(i % 2 != 0) continue;
        sum += i;
    }
    printf("Sum of even numbers = %d\n", sum);
}